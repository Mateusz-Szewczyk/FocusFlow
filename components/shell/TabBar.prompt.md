The app's three destinations.

```jsx
<TabBar current="today" onChange={go} tabs={[
  {value:'today',label:'Today'},{value:'grove',label:'Grove'},{value:'connect',label:'You'}]} />
```

Never add a fourth tab, an icon or a badge count.
