The only motion on the focus session screen: a ring breathing on a 15-second sine with a stem growing inside it.

```jsx
<BreathRing size={186} />
```

Never put a full tree on a running session — growing it is the reward, never the pressure. Do not add progress percentages, pulses or completion flourishes.
