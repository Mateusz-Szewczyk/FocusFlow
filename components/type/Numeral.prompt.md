Times and counters, monospace and tabular.

```jsx
<Numeral size="lg">18:50</Numeral>
<Numeral size="md">37:48</Numeral>
```

One big number per screen at most, and only if it changes what the user does next. Never set a time in the serif.
