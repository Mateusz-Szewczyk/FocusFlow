Exclusive views over the same content.

```jsx
<SegmentedControl value={view} onChange={setView}
  options={[{value:'week',label:'Week'},{value:'month',label:'Month'},{value:'year',label:'Year'}]} />
```

Selected fills with ink, not jade — jade is spent on the screen's one primary action.
