A mono-caps pill for switching between views or screens.

```jsx
<Chip selected>Today</Chip>
<Chip onClick={() => go('grove')}>Grove</Chip>
```

Lay chips out in a horizontally scrolling flex row with a 7px gap. Selected inverts to solid ink; there is no jade chip.
