A 1px hairline with 30px of air either side — the system's only divider.

```jsx
<Rule />
<Rule gap={22} />
```

If you are reaching for a card, a background fill or a shadow to group content, use a Rule and whitespace instead.
